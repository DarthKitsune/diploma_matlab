TODO list
+1) функция для загрузки файла с котировками. Передается путь, возвращается массив с ценами
+2) Из массива с котировками строим свечи. Передается: массив котировок, длина тика. Возвращается массив свечей. 
3) Обработка массива свечей: нормализация и расчет дополнительных параметров. Вход: массив свечей. Выход: массив нормализованных свечей с дополнительными параметрами.
4) функция, вычисляющая максимум и минимум расстояний между свечами внутри заданного массива. Вход: массив свечей. Выход: два числа	
5) функция, считающая центры кластеров. Алгоритм я привел в письме, позже тут еще раз запишу. Вход: массив свечей, выход: массив свечей.

Все это должно последовательно и аккуратно вызываться внутри одного m файла. По каждой функции должен быть отчет. 